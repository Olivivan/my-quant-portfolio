# Define the matching engine logic as a static library
add_library(hft_core_lib STATIC hft_core.cpp hft_core.h)

# Ensure headers are accessible to other modules
target_include_directories(hft_core_lib PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})