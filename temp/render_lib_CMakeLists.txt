# Define the logging and rendering module as a static library
add_library(hft_render_lib STATIC hft_render.cpp hft_render.h)

target_include_directories(hft_render_lib PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})